# 项目维护约定

## 范围与入口

- 本项目维护 Scriptable 的 AI 订阅用量小组件，当前提供 ChatGPT / Codex 和 Claude。
- 回复、文档和必要的代码注释使用简体中文。未明确指定时，日期与时间使用 Asia/Taipei（GMT+8）、24 小时制。
- 修改前阅读根目录 `README.md`、对应组件的 `README.md`、`docs/STYLE.md` 和 `docs/MAINTENANCE.md`。
- `widgets/<service>/` 中的脚本是安装与维护的源文件。每个组件保持单个可直接粘贴到 Scriptable 的 JavaScript 文件。
- `previews/` 是生成结果。修改生成来源后重新生成，不单独修补产物。
- 先阅读相关实现和调用关系，取得代码、报错或实际运行证据后再修改逻辑。不要凭猜测修复手机端问题。

## 已确定的界面要求

- 目标是 iPhone 17 Pro 及以上机型的小尺寸 2×2，不考虑更小的机型。上下 Padding 为 **12**，左右为 **14**；保持脚本顶部的常量，未经用户要求不改变。
- 第一行：左侧 **18 点粗体标题**，右侧套餐（如 `Pro 5x`、`Plus`，倍数用小写 `x`）。
- 周期行：周期名称在左（`Weekly`、`5h`），已用百分比在右，不加「已用」字样。单周期百分比 28 点，双周期 15 点。
- 进度条为 20 格等宽竖条，已用格实色、未用格浅色；单周期高 20 点，双周期高 6 点。每格圆角 1.5 点、间隙 2 点，每格是一个固定尺寸、带背景色和圆角的原生容器，由系统按屏幕分辨率矢量绘制；格宽按内容区宽度计算，格间距用容器的 `spacing`。不要改回位图（桌面渲染时会被重新采样，边缘发虚），也不要用弹性留白撑开各格（iOS 弹性留白约有 8 点最小长度，会撑破组件）。
- 重置时间左对齐。底部行左侧为成功更新时间，右侧为登录／网络等状态。进度条与文字使用相同的左右边距，不能额外缩进。
- 显示已用量。数字与进度条低于 95% 用蓝色，达到或超过 95% 同时变为橙色提醒。蓝色浅色 **#3A83F7**、深色 **#75A7FF**；橙色浅色 **#DC6F0C**、深色 **#FFA94D**。红色只用于登录过期。
- Logo 作为右下角背景，超出组件的部分裁切，尺寸 85%、偏移 -11%，尺寸与偏移以组件边长为基准。ChatGPT 不透明度 0.03；Claude 浅色 **0.04**、深色 **0.075**。
- Logo 使用官方 SVG 的路径数据，由 `DrawContext` 按矢量绘制成背景。ChatGPT 浅色黑、深色白；Claude 两种主题都用官方 Clay 色 **#D97757**。按动态颜色解析本次运行主题，切换系统外观后背景需等待脚本重新运行。
- 保持留白和字体层级。不要擅自放大元素、右对齐时间或增加统计卡片、按钮、状态标签。
- 完整尺寸、颜色、状态文案与时间规则见 `docs/STYLE.md`。

## Scriptable 与数据规则

- 手机运行环境是 Scriptable / JavaScriptCore，不具备 Node.js 的 `fs`、`require`、`Buffer` 或浏览器 DOM。Node 只用于本地预览。
- 新增或改变原生 API 用法时，先核对 Scriptable 文档；可优先用 Context7 的 `/websites/scriptable_app`，不足时查官方文档。
- **钥匙串键名不得以 `scriptable` 开头**，这是 App 的保留前缀。现有键为 ChatGPT 的 `chatgpt.usage.v1.*` 和 Claude 的 `claude.usage.v1.*`，维护时保留这些键名，避免让已安装用户丢失登录状态。
- 新服务使用独立的凭据与缓存命名空间，不复用其他服务的令牌或设备码。
- 两个组件默认请求官方接口。脚本开头的代理常量可填写可选的反向代理完整地址（包含路径），每个常量代替一个官方地址前缀：ChatGPT 的 `PROXY_AUTH_URL`（`https://auth.openai.com`）与 `PROXY_CHATGPT_URL`（`https://chatgpt.com/backend-api`），Claude 的 `PROXY_ANTHROPIC_URL`（`https://api.anthropic.com`）。不要假定代理的路径结构，也不要把某个代理的路由前缀写进脚本逻辑。在 Safari 中打开的授权网页和 `redirect_uri` 保持官方地址。仓库中的代理常量保持为空，文档示例使用 `https://proxy.example.com/...`，不写入具体节点地址。
- Claude 组件复用 Claude Code 的公开 OAuth 客户端读取未公开的用量接口，与 Anthropic 条款冲突，用户已接受此风险。保留使用说明中的条款风险与同类开源实现说明。借用电脑上 Claude Code 的访问令牌验证接口时只读，不得用其续期令牌续期，以免电脑上的 Claude Code 退出登录。
- 凭据保存到 Keychain。不得写入源码、预览、截图、日志、压缩包或提交。诊断信息只记录阶段、状态码和脱敏原因。
- HTTP 成功且数据格式正确后才保存新用量并更新 `fetchedAt`。失败时保留最后一次成功用量及原更新时间。
- 网络错误、403、429、服务器错误与登录失效分别处理。仅明确的认证失效进入重新登录流程。
- 只展示接口实际返回的额度窗口、时长和已用百分比；不能根据套餐名称猜测限额、补一个 5 小时窗口或把缺失窗口当成 0%。
- 用户已确认点击组件会进入 Scriptable 并更新用量。当前公开接口的点击运行会打开 App；后台自动刷新由 iOS 调度。不得承诺桌面原地按钮刷新、固定刷新周期或 `Script.setWidget()` 后立即重绘。

## 修改与验证

- 提交前检查当前仓库实际启用的 Git hooks 及其相关本地配置，了解它们对暂存内容的检查与自动处理。保留现有流程，不擅自跳过、禁用或覆盖 hooks；排查提交内容与工作区的差异时，将这些本地处理纳入检查。
- 只实现当前需求，不提前建立多服务插件框架、兼容层、通用登录系统或额外配置项。新增服务时先做独立组件，有实际重复需求后再讨论共享代码。
- 不主动新增单元测试等测试代码。根据改动运行相关的语法检查、预览生成和直接执行验证。
- 本项目无 npm 运行依赖，也无安装依赖步骤。若以后确需安装依赖，先检查已有 lock 文件并使用对应包管理器。
- 使用 `rg` 检索；文件统一 UTF-8、LF。生成工具的路径以项目目录为基准，不依赖当前工作目录或个人电脑上的绝对路径。
- 常用命令：

```sh
node --input-type=module --check < "widgets/chatgpt/ChatGPT Usage.js"
node --input-type=module --check < "widgets/claude/Claude Usage.js"
node tools/render-preview.cjs
```

- 样式变化检查 158 与 170 点预览、单/双周期、深浅色、94.9%/95% 和错误状态。HTML 是近似布局，不是原生渲染证明。
- 实机问题要保留并利用具体错误信息。电脑上的接口适配或模拟通过，不能写成 iPhone 原生验证通过。
- 发布前更新组件说明、重新生成预览，并核对敏感信息。总结只写最终行为、实际验证及仍需实机确认的事项。
